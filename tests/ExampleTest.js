var assert = require('assert');
describe('The First Test', function() {
  describe('#true Result', function() {
    it('should return true', function() {
      assert.equal(true, true);
    });
  });
});
